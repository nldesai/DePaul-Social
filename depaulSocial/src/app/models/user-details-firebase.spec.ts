import { UserDetailsFirebase } from './user-details-firebase';

describe('UserDetailsFirebase', () => {
  it('should create an instance', () => {
    expect(new UserDetailsFirebase()).toBeTruthy();
  });
});
